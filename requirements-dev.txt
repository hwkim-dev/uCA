# =============================================================================
# pccx — development-only dependencies
#
# Install via:
#     pip install -r requirements.txt -r requirements-dev.txt
#
# CI uses `requirements.txt` alone; local development and the lint workflow
# pull this file on top.
# =============================================================================

-r requirements.txt

sphinx-autobuild>=2024.9
sphinx-lint>=1.0
codespell>=2.3
